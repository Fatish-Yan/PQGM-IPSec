# 第五章 系统实现与性能评估
前文在理论层面对 PQ-GM-IKEv2 混合密钥交换协议进行了详细设计与形式化安全性证明。本章将从工程落地角度出发，阐述该协议在真实网络环境中的系统实现与验证过程。本研究以目前国际上支持标准最前沿的开源 IPsec 协议栈 strongSwan 6.0+ 为基础架构，通过开发国密算法插件、深度重构协议状态机与载荷解析模块，成功构建了支持 5-RTT 混合交换的原型系统。本章将详细介绍核心模块的代码重构细节，并通过实验验证协议的功能正确性，最后结合测试数据对协议的传输与计算开销进行客观评估。
## 5.1 实验环境与系统总体架构
### 5.1.1 软硬件运行环境与依赖库选型
为确保原型系统具备良好的性能与工业参考价值，本实验部署在两台基于 x86_64 架构的 Linux 服务器上，分别模拟 VPN 发起方（Initiator）与响应方（Responder）。软硬件配置如表 5-1 所示。
表 5-1 实验软硬件环境配置表
| 组件 | 规格/版本说明 |
| :--- | :--- |
| 操作系统 | Ubuntu 22.04 LTS (Kernel 5.15) |
| CPU / 内存 | Intel Core i7-12700K (分配 4 核心) / 8GB RAM |
| 基础 IPsec 框架 | strongSwan 6.0.0 (支持 RFC 9370 多重密钥交换扩展) |
| 后量子密码库 | liboqs (Open Quantum Safe项目，支持 ML-KEM) |
| 国密密码库 | GmSSL 3.1.x (提供 SM2/SM3/SM4 底层密码学实现) |
| 网络抓包工具 | Wireshark 4.0+ (支持 IKEv2 自定义报文解析) |
在依赖库选型上：strongSwan 6.0.0 是目前极少数原生支持 RFC 9370（多重密钥交换）和 RFC 9242（中间交换）框架的协议栈，这为多轮 PQC 交互提供了基础容器；liboqs 提供了 NIST 标准化的 ML-KEM 实现，并通过 strongSwan 自带的 oqs 插件接入；而对于国密算法，本研究选用 GmSSL 库，因其对 GM/T 标准协议栈支持最为完整。
### 5.1.2 原型系统总体架构
strongSwan 采用用户态密钥协商守护进程（Charon）与内核态数据包处理（Linux XFRM）相分离的模块化架构。本研究对协议的改造完全集中于用户态的 Charon 进程。
为了实现 PQ-GM-IKEv2 协议，本研究遵循 strongSwan 的松耦合插件架构，对系统进行了如下侵入性改造（改造架构分布如图 5-1 所示）：
1. 加密组件层（Crypto API）：新增开发 gmssl_plugin 模块，将 GmSSL 封装为标准 crypter_t, hasher_t 和 signer_t 接口，并针对私有协商 ID 60001 定制了特殊的密钥交换接口。
2. 载荷与协议层（Payload & Protocol）：修改 payload_parser，增加对 IKE_INTERMEDIATE 报文中双证书并发解析的支持；修改 proposal 处理模块，放宽对私有 Transform ID 的识别限制。
3. 状态机任务层（Task Manager）：这是改造最深的部分。修改 ike_intermediate.c 与 ike_auth.c，重构证书的收发时序与验证逻辑，并在 keymat_v2 模块中接入自定义的国密级联派生逻辑。
**
## 5.2 协议栈核心模块的深度重构与实现
虽然 strongSwan 6.0 引入了对多重密钥交换的基础支持，但其内置逻辑严格假定“所有交换均为基于 DH 的密钥协商（Key Agreement）”以及“证书仅存在于身份认证阶段”。为了融合国密“密钥传输”特色与早期门控逻辑，本研究对底层核心模块进行了深度重构。
### 5.2.1 异构密码算法底座的插件化接入
strongSwan 的加密工厂（Crypto Factory）通过枚举类型（如 HASH_SM3, ENCR_SM4_GCM）来分发算法调用。本研究首先在 src/libstrongswan/plugins/ 目录下创建了全新的 gmssl 插件工程。
针对国密算法底座的搭建，主要完成了以下接口适配：
● 杂凑与对称加密：实现 hasher_t 接口封装 sm3_digest()；实现 aead_t 接口封装 sm4_gcm_encrypt()，使其支持 IPsec ESP 载荷的处理。
● 公钥密码与签名：实现 public_key_t 和 private_key_t 接口，封装 SM2 椭圆曲线操作，重点重写了 sign_t 和 verify_t 方法以支持带 ID 的 SM2 签名机制。
● 后量子算法适配：直接编译并启用 strongSwan 源码树中的 oqs 插件，在配置文件中映射 ML-KEM-768 (Transform ID 35) 与 ML-DSA-65，从而无缝衔接至算法工厂。
### 5.2.2 证书预分发与早期门控状态机的重构
传统 IKEv2 的证书处理被硬编码隔离在 ike_auth.c 任务中。本协议要求在 IKE_INTERMEDIATE R0 阶段收发双证书，并执行有效性验证作为抗 DoS 门控。这一时序颠覆了 Charon 守护进程的原有假设。
为实现该特性，本研究对状态机（Task Machine）进行了如下重构：
1. 强制载荷注入：在 ike_intermediate.c 的 build_i / build_r 方法中，注入原本属于 AUTH 阶段的 cert_payload_t 和 certreq_payload_t 构建代码。通过遍历本地凭证管理器（Credential Manager），将配置好的 SM2 签名证书与加密证书序列化后附加至报文中。
2. 双证书连续解析：修改载荷解析器，使其能够在收到 R0 报文时，连续解析出多张 X.509 证书，并扩展了会话上下文 ike_sa_t 的数据结构，将 sig_cert 和 enc_cert 分别挂载至内存中备用。
3. 早期门控拦截器：在 R1 状态的入口处，新增安全断言拦截机制。系统主动调用 credential_manager->verify_trust_chain() 对 R0 收到的 SM2 签名证书进行信任链及 CRL 校验。一旦底层返回 AUTH_FAILED，协议栈立即主动销毁 ike_sa 对象，终止状态机，从而在进入后续 PQC 计算任务前形成了有效的防线。
### 5.2.3 SM2-KEM 数字信封的“协商”接口逆向适配
实现国密密钥交互的最大工程挑战在于：strongSwan 6.x 的 key_exchange_t 抽象接口是为 Diffie-Hellman 类算法（包括 ML-KEM 等 KEM 算法在 IKEv2 的应用）设计的，其标准调用流为双方各自调用 get_public_key() 发送公钥，再调用 get_shared_secret() 生成秘密。而国密数字信封本质上是“发起方使用响应方公钥加密种子”的单向操作。
为了将国密逻辑强行“塞入” RFC 9370 的多重交换框架，本研究针对私有标识 ID 60001 开发了一个极其特殊的 sm2_kem_exchange_t 类对象，采用了“上下文透传与异步解耦”的实现技巧：
● 发起方逻辑重载：当触发 get_public_key() 构造 R1 载荷时，发起方并不生成自己的公钥。而是从 ike_sa_t 上下文中提取 R0 阶段缓存的对端加密证书（EncCert）公钥。利用该公钥调用 GmSSL 加密随机种子 $r_i$，并将密文 ct_i 伪装成 public_key 返回给载荷构建器。
● 响应方逻辑重载：响应方收到 ct_i 后，在 set_public_key() 函数内部拦截该密文，并立刻使用本地的加密私钥解封提取出 $r_i$；随后在构造自身载荷时，生成 $r_r$ 并使用对端 EncCert 封装回传。
● 混合密钥注入：完成交换后，双方的 get_shared_secret() 函数输出组合拼接的 $SS_{gm}$。Charon 内置的 keymat_v2->derive_keys() 将自动捕获此输出，并结合后续 ML-KEM 产生的 $SS_{pqc}$，在循环中完成多次 $SKEYSEED$ 的级联扩展更新，从而顺畅复用了开源协议栈自身的密钥管理流。
### 5.2.4 基于长周期哈希累加器的后量子认证绑定
在 5-RTT 流程中防止降级攻击的核心，是在最终的 IKE_AUTH 中使用后量子签名（如 ML-DSA）对所有中间报文进行 Transcript 绑定（IntAuth）。
得益于 strongSwan 6.x 对 RFC 9242 的原生支持，系统底层已经实现了一个跨多轮请求周期的 transcript_hash_ctx 累加器。每接收和发送一段报文二进制流，该累加器都会调用 hash_t->allocate_bytes() 持续更新摘要。
本研究在此基础上的主要工作量为认证策略的调度改造：
通过深度修改 auth_cfg_t 认证策略选择器，允许配置复杂的多级认证规则。即：配置 ike_sa 在早期阶段（R0/R1）调用 gmssl_plugin 校验基于 SM2 的证书链属性，而在最后生成 AUTH 载荷（build_auth）时，强制重定向上下文到 oqs 插件，调用 ML-DSA-65 签名算法对累加器的最终哈希值及双方 ID 执行签名操作。这一改动打通了国密预认证与 PQC 终极认证的隔阂，实现了逻辑上的闭环。
## 5.3 协议功能性验证
在完成核心模块的代码重构后，本节通过在真实局域网环境下运行原型系统，结合网络抓包与守护进程日志，对 PQ-GM-IKEv2 协议的功能正确性进行验证。
### 5.3.1 5-RTT 交互时序与载荷解析验证
启动发起方与响应方的 strongSwan 服务，并配置强制混合模式（Profile A：SM2 + ML-KEM-768）。在通信链路上使用 Wireshark 进行抓包，捕获到的 UDP 500 端口通信序列如图 5-2 所示。
**
从抓包时序中可以清晰地观测到本协议设计的 5-RTT 流水线特征：
1. Message 1-2 (IKE_SA_INIT)：明文交换，载荷中包含了标识中间交换支持的 Notify 载荷，以及包含私有 ID 60001 和 ML-KEM ID 35 的 SA 提案。
2. Message 3-4 (IKE_INTERMEDIATE R0)：加密信道建立后的第一次中间交换。抓包显示报文类型为 43（Intermediate），且长度显著增加，这证实了内部封装了双证书链（Cert Payload）。
3. Message 5-6 (IKE_INTERMEDIATE R1)：第二次中间交换，承载基于 ID 60001 的 SM2 数字信封交互。
4. Message 7-10 (IKE_INTERMEDIATE R2)：第三次中间交换，执行 ML-KEM。由于载荷超过了配置的 1280 字节 MTU，Wireshark 捕获到了连续的 IKE_SKF（Encrypted Fragment Payload）分片报文，证明应用层分片机制被正确触发并运作良好。
5. Message 11-12 (IKE_AUTH)：最终的认证报文，成功建立了 Child SA。
### 5.3.2 证书链校验与会话建立验证
为了进一步验证内部状态机的流转逻辑，我们提取了响应方 Charon 守护进程的关键运行日志（Log Level 2）。以下截取了部分核心日志输出：
# 1. 证书预解析与早期门控验证
08[IKE] processing IKE_INTERMEDIATE request 1
08[CFG] received issuer cert "C=CN, O=GM-Test, CN=Root CA"
08[CFG] received end entity cert "C=CN, O=GM-Test, CN=Initiator SignCert"
08[CFG] received end entity cert "C=CN, O=GM-Test, CN=Initiator EncCert"
08[CFG] using certificate "C=CN, O=GM-Test, CN=Initiator SignCert"
08[CFG]   reached self-signed root ca with a path length of 0
08[IKE] Early Gating: SM2 certificate trust chain verified successfully.

# 2. SM2 数字信封解密与第一轮密钥更新
10[IKE] processing IKE_INTERMEDIATE request 2
10[IKE] extracting encapsulated seed from KE payload (ID 60001)
10[IKE] decapsulating SM2 enveloped data using local EncCert private key
10[IKE] updating SKEYSEED with GM shared secret

# 3. PQC 密钥封装与第二轮密钥更新
12[IKE] processing IKE_INTERMEDIATE request 3
12[IKE] decapsulating ML-KEM-768 ciphertext
12[IKE] updating SKEYSEED with PQ shared secret

# 4. 最终身份认证与 SA 建立
14[IKE] processing IKE_AUTH request 4
14[IKE] verifying ML-DSA signature over transcript hash (IntAuth)
14[IKE] authentication of 'Initiator' successful
14[IKE] IKE_SA established successfully
14[IKE] CHILD_SA established with SPIs c4e5d6f7_i 9a8b7c6d_o
日志链路清晰地印证了 5.2 节中描述的底层重构逻辑：证书确实在 R0 阶段被解析，早期门控成功放行，SM2 和 ML-KEM 的共享秘密被依次用于更新 SKEYSEED，最终的 Transcript 签名验证通过，IPsec 隧道成功建立。
## 5.4 系统性能评估与开销分析
协议引入国密双证书与抗量子算法必然带来额外的通信和计算开销。本节在千兆局域网环境下（RTT < 1ms，以隔离网络抖动对计算测试的干扰），对标准 IKEv2（ECDH-P256 + RSA-2048）与本文的 PQ-GM-IKEv2（SM2 + ML-KEM-768）进行性能对比评估。
### 5.4.1 通信开销与分片效率测试
通过 Wireshark 统计一次完整握手过程中各轮次在网络层（包含 UDP/IP 头部）的实际传输字节数，对比结果如表 5-2 所示。
表 5-2 IKEv2 协议各阶段通信开销对比（单位：字节）
| 交互阶段 | 传统 IKEv2 (ECDH+RSA) | PQ-GM-IKEv2 (SM2+ML-KEM) | 差异分析 |
| :--- | :--- | :--- | :--- |
| IKE_SA_INIT | ~ 450 | ~ 520 | 增加额外的 Transform ID 提案 |
| R0 (双证书分发) | N/A (0) | ~ 2450 | 包含两张 SM2 证书及 CA 证书链 |
| R1 (SM2 数字信封)| N/A (0) | ~ 280 | SM2 密文及 KE 载荷头部 |
| R2 (ML-KEM 交换)| N/A (0) | ~ 2580 | 发起方公钥(1184B) + 响应方密文(1088B) + 分片头部 |
| IKE_AUTH | ~ 1250 (含证书) | ~ 550 (无证书) | 证书已前置，此处仅包含 PQ 签名及子 SA 载荷 |
| 总交互流量 | ~ 1700 Bytes | ~ 6380 Bytes | 协议总体数据量增长约 3.7 倍 |
分析结论：
1. 载荷显著增加：PQ-GM-IKEv2 的总通信量达到了约 6.3 KB，相比传统 IKEv2 增长了约 3.7 倍。主要的增量来自于国密双证书链（约 2.4 KB）以及 ML-KEM 的公钥与密文对（约 2.5 KB）。
2. 分片机制的高效运作：在实验设定的 1280 字节 MTU 下，R0 阶段的证书报文和 R2 阶段的 PQC 发起方报文均超过了单包限制。原型系统成功将其切分为 2-3 个 IKE_SKF 分片传输，且未发生 IP 层分片，证明了 3.5 节所设计的应用层分片策略在应对载荷膨胀时具备极高的工程可靠性。
### 5.4.2 握手时延与计算开销对比
为了评估计算密集型任务（如大矩阵乘法与椭圆曲线点乘）对建立连接速度的影响，我们对网关发起了 1000 次连续的新建连接请求，统计从发送第一个 IKE_SA_INIT 到成功收到 IKE_AUTH 响应的握手总时延（Handshake Latency），取平均值作为评估指标。
表 5-3 协议握手时延对比
| 测试场景 | 握手轮次 | 平均时延 (ms) | 说明 |
| :--- | :--- | :--- | :--- |
| 传统 IKEv2 (Baseline) | 2-RTT | 21.5 | 仅包含一次 ECDH 交换与一次 RSA 验签 |
| PQ-GM-IKEv2 (混合模式) | 5-RTT | 98.4 | 包含双证书解析、SM2信封加解密、ML-KEM加解封及多次 PRF |
分析结论与抗 DoS 验证：
实验数据显示，PQ-GM-IKEv2 的完整握手时延约为 98 毫秒，是传统 IKEv2（约 21 毫秒）的 4.5 倍左右。考虑到协议增加了 3 个完整的 RTT（带来网络 I/O 延迟增加），同时叠加了国密公钥运算与后量子格密码的计算开销，该时延增长完全符合理论预期。在人类可感知的范围内（< 100ms），此级别的连接时延对于绝大多数 VPN 拨号接入场景是完全可以接受的，牺牲不到 80 毫秒的时间换取了长远的“抗量子机密性”与“国密合规性”，具有极高的安全效费比。
此外，为了验证 “早期门控（Early Gating）” 的抗 DoS 能力，我们模拟了携带非法证书（证书过期）的攻击请求。测试表明，响应方在完成 IKE_SA_INIT 和 R0 阶段的证书验证后立即熔断连接，平均耗时仅为 34.2 ms。这一数据证明，响应方成功节省了后续 R2 阶段昂贵的 ML-KEM 解封装计算资源，使得攻击者的资源消耗代价未能转化为服务器的算力瓶颈，协议在可用性防御方面达到了设计目标。
# 5.5 本章小结
本章详细阐述了 PQ-GM-IKEv2 协议原型系统的工程落地过程。针对开源基础框架的局限性，本研究深度重构了 strongSwan 6.0 守护进程的底层状态机，不仅成功开发了异构密码算法（GmSSL 与 liboqs）的适配插件，还突破性地实现了国密证书收发前置、数字信封时序逆向适配以及基于长周期摘要累加器的后量子认证绑定。
功能性测试通过抓包与日志分析证实了 5-RTT 交互流水线与各级状态更新逻辑运转正常。性能评估表明，尽管多重密码技术导致通信开销增加约 3.7 倍、握手时延增加约 4.5 倍，但整体连接时间仍控制在 100 毫秒以内的可用范围内；更为关键的是，早期门控机制被证实能够有效阻断非法的 PQC 计算请求，证明了协议在复杂网络环境中具备良好的鲁棒性与工业应用潜力。